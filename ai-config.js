window.LUMI_AI_CONFIG = Object.freeze({
  // Same-origin server endpoint. The server keeps the Hugging Face credential
  // private and forwards requests to the existing ZeroGPU Lumi AI Space.
  endpoint: "/api/chat"
});
